<UserControl x:Class="Labb3_Quiz.Views.PlayerPointView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_Quiz.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" >

        <TextBlock Text="{Binding PointText}"
                   FontSize="30" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0 0 0 20"/>

        <Button Content="Retry Quiz"
                Command="{Binding RestartQuizCommand}"
                Width="90" Height="45" FontSize="15"/>
    </StackPanel>

</UserControl>
