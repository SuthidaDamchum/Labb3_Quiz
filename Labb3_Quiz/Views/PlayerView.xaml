<UserControl x:Class="Labb3_Quiz.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Labb3_Quiz.Views"
             xmlns:viewmodels="clr-namespace:Labb3_Quiz.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:PlayerViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20">

        <TextBlock Text="{Binding SecondsLeft}" FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Right" VerticalAlignment="Top"/>

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock Text="{Binding CurrentQuestion.Query}" FontSize="24" FontWeight="Bold"
                       TextAlignment="Center" Margin="0 0 0 20" TextWrapping="Wrap"/>

            <TextBlock Text="{Binding CurrentQuestionText}" 
               FontSize="18" FontWeight="Normal"
               TextAlignment="Center"
               Margin="0 0 0 20" />

            <ItemsControl ItemsSource="{Binding Alternatives}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="2"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>

                        <Button Command="{Binding DataContext.CheckAnswerCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding}" Margin="5" Height="50" Width="150" FontSize="18" HorizontalContentAlignment="Stretch"
    VerticalContentAlignment="Stretch" >
                            <Grid>
                                <Image Source="{Binding Asset}"
                                       Margin="-10"
                                       Width="25" Height="25" 
                                       HorizontalAlignment="Left" VerticalAlignment="Top" 
                                       Visibility="{Binding ShowCorrectIndicator, Converter={StaticResource BoolToVis}}"/>

                                <Image Source="pack://application:,,,/Assets/incorrect.png"
                                     
                                       Width="25" Height="25" 
                                       Margin="-10"
                                       HorizontalAlignment="Left"  VerticalAlignment="Top"
                                       Visibility="{Binding ShowIncorrectIndicator, Converter={StaticResource BoolToVis}}"/>
                                <TextBlock Text="{Binding Answer}" 
                                           HorizontalAlignment="Center" VerticalAlignment="Center" 
                                           TextWrapping="Wrap" FontWeight="Light"/>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Grid>
</UserControl>


